// examples/demo.ltl — runnable demo for ltl_lsm_tree

import std::io
use ltl_lsm_tree as lib

fn main() {
    io::println("ltl_lsm_tree demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}